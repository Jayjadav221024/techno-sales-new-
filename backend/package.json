{
  "name": "techno-sales-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix server run dev",
    "start": "npm --prefix server run start",
    "seed": "npm --prefix server run seed",
    "test": "node shared/test.js && node server/utils/listQuery.test.js && node server/models/softDelete.test.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
